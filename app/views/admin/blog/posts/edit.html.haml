.page-header
  %h2= "Editing #{@post.title.titleize}"

=comfy_form_for @post, :as => :post, :url => {:action => :update}, :html => {:multipart => true}  do |form|
  =render 'post_form', :form => form

